import SectionTitle from "../../../Components/SectionTitle";


const PaymentHistory = () => {
    return (
        <div>
            <SectionTitle heading={'Payment history'} subHeading={'this is payment history'}></SectionTitle>
            <div>
                ok
            </div>
        </div>
    );
};

export default PaymentHistory;